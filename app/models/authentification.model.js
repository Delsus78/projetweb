const sql = require("./db.js");

const Authentification = () => {};

Authentification.login = (_, result) => {

};

Authentification.register = (newClient, result) => {

};

module.exports = Authentification;